[package]
name = "focale-buildinfo"
description = "Build provenance strings (release version + git hash, platform) for the Focale binaries"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true

[dependencies]
